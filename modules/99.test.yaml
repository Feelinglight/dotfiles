- defaults:
    shell:
      stdout: true
      stderr: true

# - shell:
#     - command: |
#         ls .
#       description: Install packages

- link:
    ~/.config/nvim:
        path: config/lazyvim

